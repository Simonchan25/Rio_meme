# OS / IDE noise
.DS_Store
.idea/
.vscode/
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Bundled artifacts that don't belong in git
# (start.command auto-downloads the .task models on first run;
#  python-installer.pkg is a 44MB binary that should live in a release, not the tree)
*.task
python-installer.pkg
